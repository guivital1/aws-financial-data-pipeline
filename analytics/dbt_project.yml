name: financial_analytics
version: "1.0.0"
config-version: 2

profile: financial_analytics
model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  financial_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  financial_analytics:
    +quote_columns: false
